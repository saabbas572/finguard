{
  "name": "finguard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --exec ts-node src/index.ts",
    "test": "cross-env NODE_ENV=test node --require ts-node/register --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/node": "^25.9.3",
    "@types/supertest": "^7.2.1",
    "concurrently": "^8.0.0",
    "cross-env": "^10.1.0",
    "supertest": "^7.2.2"
  }
}
